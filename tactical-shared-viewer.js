(()=>{const wait=()=>{const c=window.firemapCtx;if(!c?.map||!c?.sb){setTimeout(wait,300);return}init(c)};function init({map,sb}){if(window.__sharedTacLiveV2)return;window.__sharedTacLiveV2=true;const layers=new Map(),styles={SECTOR:{color:'#facc15',weight:4,fillColor:'#facc15',fillOpacity:.08},WATER:{color:'#2563eb',weight:5,dashArray:'10,6'},DANGER:{color:'#dc2626',weight:3,opacity:.72,fillColor:'#dc2626',fillOpacity:.045},FIRE_SPREAD:{color:'#f97316',weight:5,dashArray:'8,5'},FREEHAND:{color:'#a855f7',weight:4}};const esc=s=>String(s||'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));function isPoint(d){return d.category==='POINT'||d.style?.point===true||d.label==='Punkt taktyczny'}function remove(id){const l=layers.get(id);if(l){try{map.removeLayer(l)}catch{}layers.delete(id)}}function render(d){remove(d.id);const g=d.geometry||{};let st={...(styles[d.category]||{}),...(d.style||{})};if(d.category==='DANGER')st={...st,color:'#dc2626',weight:3,opacity:.72,fillColor:'#dc2626',fillOpacity:.045};let l;if(isPoint(d)&&d.geometry_type==='CIRCLE'){const color=st.pointColor||st.fillColor||st.color||'#059669',name=st.pointName||d.label||'Punkt taktyczny',desc=st.pointDescription||'';l=L.marker(g.center,{icon:L.divIcon({className:'',html:`<div style="width:28px;height:28px;border-radius:50%;background:${color};border:3px solid white;display:grid;place-items:center;color:white;box-shadow:0 3px 10px #0008">●</div>`,iconSize:[30,30],iconAnchor:[15,15]})});if(name)l.bindTooltip(esc(name),{permanent:true,direction:'top',offset:[0,-15],className:'tac-point-label'});l.bindPopup(`<b>${esc(name)}</b>${desc?`<br>${esc(desc)}`:''}<br><small>Taktyka KDR</small>`)}else if(d.geometry_type==='CIRCLE')l=L.circle(g.center,{...st,radius:g.radius});else if(d.geometry_type==='POLYGON')l=L.polygon(g.points||[],st);else l=L.polyline(g.points||[],st);l.addTo(map);if(!isPoint(d)&&d.label)l.bindPopup('<b>Taktyka KDR</b><br>'+esc(d.label));layers.set(d.id,l)}let refreshing=false,pending=false;async function refresh(){if(refreshing){pending=true;return}refreshing=true;try{const {data:incs,error:ie}=await sb.from('incidents').select('id').eq('active',true);if(ie)return;const ids=(incs||[]).map(x=>x.id);if(!ids.length){[...layers.keys()].forEach(remove);return}const {data,error}=await sb.from('incident_drawings').select('*').in('incident_id',ids).order('created_at');if(error)return;const rows=data||[],keep=new Set(rows.map(x=>x.id));[...layers.keys()].forEach(id=>{if(!keep.has(id))remove(id)});rows.forEach(render)}finally{refreshing=false;if(pending){pending=false;setTimeout(refresh,80)}}}const ch=sb.channel('vehicle-tactical-live-v2').on('postgres_changes',{event:'INSERT',schema:'public',table:'incident_drawings'},p=>{if(p.new)render(p.new);setTimeout(refresh,120)}).on('postgres_changes',{event:'UPDATE',schema:'public',table:'incident_drawings'},p=>{if(p.new)render(p.new);setTimeout(refresh,120)}).on('postgres_changes',{event:'DELETE',schema:'public',table:'incident_drawings'},p=>{if(p.old?.id)remove(p.old.id);setTimeout(refresh,120)}).on('postgres_changes',{event:'*',schema:'public',table:'incidents'},()=>setTimeout(refresh,120)).subscribe(status=>{if(status==='SUBSCRIBED')refresh()});setInterval(refresh,2500);document.addEventListener('visibilitychange',()=>{if(!document.hidden)refresh()});window.addEventListener('focus',refresh);window.addEventListener('online',refresh);refresh()}wait()})();